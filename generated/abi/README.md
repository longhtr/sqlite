# ABI layout facts

Target-native C probes generate these facts from the pinned `sqlite3.h`. A target is not ABI-supported until it has native facts and matching Zig layout tests. Cross-compilation never reuses host facts.
