# Pinned Zig/toolchain probes

These probes preserve answers to compatibility-critical Zig 0.16 and C ABI questions. They are built with the exact target toolchain. A future Zig or target update must rerun them before changing checksums.
