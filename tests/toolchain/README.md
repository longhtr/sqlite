# Pinned Zig/toolchain probes

These probes preserve answers to compatibility-critical Zig and C ABI questions. They are built with the exact pinned target toolchain. A future toolchain or target update must rerun them before changing checksums.
