# Native SQLite port operational memory

The goal is a complete, source-faithful port of the pinned SQLite core to production Zig, with a Zig-native API, SQLite-compatible behavior and formats, and zero C implementation objects in production artifacts. Preserve honest incompleteness until machine evidence supports stronger claims.

Treat this file as live operational memory and rewrite it whenever that improves execution. After a user correction, execution failure, changed constraint, or newly discovered failure mode, consolidate the rule here before other work; remove obsolete/conflicting text instead of appending endlessly.

1. **Priority and continuity.** Spend at least 95% of normal execution on pinned-source research or production Zig porting. Explicit user-requested repository audits and incidents are temporary exceptions. Work autonomously until the user explicitly says stop, user action is genuinely required, or the project is machine-verified complete. Never stop, pause, emit a final response (including an empty final response), send an acknowledgement-only response such as "Understood" or "Continuing", or report routine progress merely because a batch or checkpoint completed; immediately begin the next qualifying source batch and continue across commits. Running out the current response budget is not a stopping condition: preserve enough budget to start another tool-backed batch rather than attempting to conclude the turn. A verified batch boundary, successful broad test, clean working tree, completed push, or user question about why work stopped is only a safe internal transition point, not permission to return control; answer such corrections by updating this memory and continuing tool execution, never by concluding the turn.

2. **Admission and counting.** Before implementing/counting a function: inspect the actual `upstream/source-inventory.json` container (`entities` is a list) and exact field names in a separate completed query before writing any consumer (the source key is `file`, not a guessed `source_file`); query the exact ID against `upstream/active-port-batch.json`; read the complete upstream body; search all production Zig repository-wide by C identity and intended Zig name/behavior before editing; inspect nearby owners and every source-identity comment. Count only active-profile, net-new source-corresponding production behavior—never inactive/not-emitted entities, tests, adapters, wrappers around existing behavior, mappings, generated artifacts, prose, renames, or pre-existing functions. Copy IDs and source hashes from the inventory query; never infer line suffixes or source hashes. Remove invalid credit immediately. An unqualified `zig_function` regex match is not identity evidence: new credit also requires one exact AST declaration ID/hash, a synchronized canonical symbol-map target, and assignment to a dependency-closed atomic unit. When assigning a previously legacy-classified source identity to a dossier, synchronize its canonical classification to the dossier's evidence state even if the old label sounded more advanced; retain the old evidence details, since atomic-unit validation requires one authoritative state.

3. **Checkpoint gate.** There are currently **zero accepted translation checkpoints and twenty-seven active entries (twenty substantive and seven short)**. The retired quantity gate's 1,738 claims (1,566 substantive, 172 short) are formally reconciled with zero completion credit: 1,188 are `unmapped`, 1,333 have no canonical target, only 104 are assigned to atomic units, 269 conflict with canonical targets, and the 136 matching targets still lack durable pre-gate evidence. All 67 dependency-ordered dossiers have structured source context. Sixteen dependency-closed runtime units are source-translated, 31 are internal-trace-equivalent and 19 dependency-closed foundational/runtime units are subsystem-integrated; none is assurance-passed yet. Process initialization resets and rebuilds the built-in hash under the recursive lifecycle lock before PCache and OS initialization. The canonical table has exact pinned-C bucket, hash-head, overload, arity, flag, and integer-user-data topology for all 178 active-profile definitions with no extras. The two load-extension and nine ALTER TABLE records use explicit erroring fallback callbacks in the internal registry, so their callable internal VDBE behavior remains open even though the full-table topology differential is exact. Shared memdb stores allocate and free their dynamic FAST mutex through the configured process mutex subsystem; allocation failure rolls open back before registry publication, while private stores retain the source null/no-op mutex path. Pager rollback journals route through the typed memory-journal owner and its embedded v1 `sqlite3_file` method table, including memory-only rollback, immediate-real VFS transport, playback, sync, close, and file-control exposure. The reconciliation freeze is lifted. Historical reconciliation fields are an immutable snapshot: later canonical mapping or atomic-unit promotion must not rewrite or invalidate that snapshot. A retired source identity may receive future credit only for demonstrably post-retirement production behavior against exact retired declaration hashes, never for restoring its old path/name claim. Resume net-new source translation only through dependency-closed atomic units and the exact active-batch ledger. A future checkpoint must add either 200 short or 50 substantive net-new functions and pass `zig build port-batch-checkpoint` with exact source/declaration hashes, canonical ownership, promoted atomic-unit assignment, and durable evidence. Ordinary builds and read-only controls remain available below threshold.

4. **Commands and authorization.** Tests/builds/verifiers are authorized when useful, including `tools/verify_config.py`, `tools/generate_behavioral_inventory.py`, `tools/port_audit.py`, and `tools/compatibility_report.py`; avoid iterative verifier loops. Generators, commits, and pushes still need their own justification/request. Classify each command as source research, production porting, requested audit, or checkpoint validation; otherwise do not run it. Every exploratory `rg` whose no-match result is acceptable (including each command in a chain) ends with `|| true`. Pass shell patterns containing Markdown backticks through a quoted heredoc or single-quoted data, never a double-quoted command where backticks trigger command substitution. Mechanically balance inline-Python delimiters; use a heredoc rather than escaped-newline control flow in `python -c`, and never pipe input into a heredoc-backed Python command because the heredoc already owns stdin. Probe script existence independently so a removed sibling cannot suppress a valid invocation.

5. **Validation cadence.** Prefer one broad pass plus only targeted repairs. Do not rerun an unchanged umbrella suite or impose arbitrary short timeouts. Preserve passing runtime/differential evidence when status/docs/tooling checks fail. If a broad run passes behavior but reports stale declarations, do not rerun it unchanged: regenerate declaration inventory, then other changed generated inputs, then dependent status. Run `port_audit.py --write` before `verify_config.py`. When `Opcode` changes, regenerate opcode coverage and synchronize its single prose summary in `docs/CURRENT_STATE.md`.

6. **Integration and claims.** Port coherent subsystems and connect real production paths. Aggregate parsing treats `count(*)` as zero arguments only when the star immediately precedes the closing parenthesis. Invocation shims for collation-aware built-ins must provide a valid context `iOp` and preceding operation `p4.pColl`; `minMaxStep()` interprets non-null user data as maximum, not minimum. When both operands of a partial-index comparison carry explicit COLLATE clauses, the left operand's explicit collation wins; consume the right clause without overwriting that precedence. New empty databases use the configured 4,096-byte default page size and encode the empty page-1 B-tree offsets for that size; never preserve the old 512-byte convenience image. A deserialized READONLY memdb remains a writable-open B-tree whose VFS rejects upgrades above a shared lock even on the private persistent MemoryVfs backend, so `sqlite3_db_readonly()` reports 0 while writes return read-only like upstream. Private-memdb serialization reports the store size before attempting the copy allocation, so copy OOM returns null while preserving the exact size output. Ordinary database serialization reads the connection's live Pager page count and page size, copies each successful page, zero-fills failed page gets, and rejects NOCOPY; never reopen and copy the raw file behind the Pager snapshot. Deserialize publishes the transferred MemoryVfs image before Pager validation; malformed input returns success from deserialize, fails on first non-empty prepare, preserves exact NOCOPY ownership, and remains safely closable. Backup's serialize/deserialize bridge must pass `FREEONCLOSE` and transfer the copied image; deferring a caller free leaves the lazily opened destination pointing at released storage. Backup handles own stable copies of both schema names, route attached source/destination images through those names, retain an attached source owner so DETACH fails until finish, and preserve one owned source snapshot across bounded positive-page steps, derive page-count progress from the selected source Pager page size rather than a fixed 4,096-byte assumption, restart the snapshot when the selected source Pager records writes between steps, reject deserialize replacement while that schema is retained as a backup source, and transfer it on the completing step. Anonymous deserialized databases use a private persistent MemoryVfs backend, not named-memdb last-close semantics. Stage and validate deserialize backend ownership, configured maximum, and initial truncation before retiring the existing database; pre-publication failure must preserve old queryable content and release FREEONCLOSE input exactly once. Retain the allocator-manager identity with adopted external storage so resize and final free stay in the original domain; realloc failure preserves pointer, content, logical size, and capacity. A named shared memdb registry entry must retain an anchor file handle for the store lifetime; otherwise closing the initializer's final transient VFS handle destroys the image before the first pager opens it. Every public open path that initializes `Connection` directly must activate the built-in function families just like `Connection.create()`; otherwise public prepared aggregate/scalar lookup fails even though internal tests pass. Scalar functions invoked by database-less prepared programs must not assume `sqlite3_context.pOut.db` is non-null; use the profile limit fallback when only a limit is needed. Report compilation, differential evidence, integration, and assurance separately. Registration is not integration until prepared SQL resolves and executes it. When broadening a shared expression resolver or declared affinity, validate schema expression refill, DML-time schema reparsing, and partial-predicate creation/DML paths separately, and validate column-first and reversed operand orders independently; a passing CREATE/refill, expression-index, or column-first path does not establish DML, partial, or reversed integration. The configured primary schema name and the permanent `main` alias must both resolve through every SQL and public schema lookup after `SQLITE_DBCONFIG_MAINDBNAME`. Public filename APIs return the user-visible database filename, never an internal storage alias such as the memdb key. Table-column metadata and file-control APIs resolve the requested attached schema, while connection-wide cache status/release/flush visits every opened attached pager; native CACHE_USED meters owned page objects rather than upstream Pager/Btree structural allocations, so only API/result/reset semantics are cross-engine evidence for that counter; metadata failures zero all outputs and nullable column names perform table-existence checks. DETACH while an incremental blob is open on that attached schema returns generic logic error, not BUSY, and must leave the blob/database alive. After a failed incremental-blob seek, the handle is permanently invalidated: subsequent read, write, and reopen calls return abort until close, matching `blobSeekToRow()` and `sqlite3_blob_reopen()`. Expanded SQL renders unbound parameters as `NULL`; do not preserve placeholder text in stale fixtures. If preparation returns generic logic error, inspect initialization/lookup first. A recognized expression helper or operator branch must distinguish malformed or out-of-range arguments from "not this expression"; returning null or leaving trailing tokens for a recognized invalid form may let a later generic branch accept it. Native probes prepare/step statements separately before interpreting multi-statement `sqlite3_exec()` failure. Bounded UPDATE currently accepts one assignment per statement; differential fixtures for predicate transitions must change one column at a time rather than misattributing a generic logic error from a multi-assignment SET list to predicate evaluation. In attached-deserialize allocation differentials, inject replacement OOM before the first schema-qualified query opens the pending B-tree; querying first changes the native allocation path and invalidates the intended pre-publication comparison. When DDL integration on a small-page fixture returns `too_big`, check whether the leaf-only schema writer exhausted page capacity before blaming SQL parsing; use a non-auto-vacuum fixture large enough for the intended schema. Pager `.reader` is the native post-statement cache state, not evidence of an open SQL read transaction; `sqlite3_txn_state()` reports NONE after autocommit operations on main or attached schemas. Preserve the connection's live result-code mask across prepared statements: step, reset, finalize, and therefore exec expose primary codes by default and extended codes only after enabling them. Memory ATTACH/DETACH owns stable heap-backed AttachedDatabase objects through `attachment_runtime`; the catalog always reserves indices 0 and 1 for main and temp, enforces the attached limit and duplicate names, and destroys native backends on detach or connection close. Serialize/deserialize, database metadata, schema-qualified table scans, and bounded INSERT/UPDATE/DELETE resolve attached schemas through that catalog and operate on the selected attached B-tree. Attached foreign-key-enabled DML resolves parent/child tables inside the selected attached B-tree and preserves immediate constraint plus delete/update CASCADE, SET NULL, SET DEFAULT, and RESTRICT behavior for inline eligible parent keys. Bounded UPDATE accepts an INTEGER PRIMARY KEY target, validates and moves the rowid under the mutation batch, and supplies distinct OLD/NEW rowids to foreign-key checks and actions. Qualified ordinary CREATE/DROP TABLE selects the attached B-tree and stores unqualified canonical CREATE SQL; attached scan evidence includes bounded column/planned and aggregate scans; ordinary single- and multi-column CREATE/DROP INDEX, bounded optionally parenthesized unary-plus identity, unary-minus/`NOT` INTEGER, and either-order INTEGER-or-REAL-column/signed-integer-or-float add/subtract/multiply/divide, integer-coerced signed-integer-or-float remainder/bit-and/bit-or/left-shift/right-shift, plus column bit-not, `abs(INTEGER-or-REAL-column)`/`sign(INTEGER-or-REAL-column)`/`round(INTEGER-or-REAL-column)`/two-argument `round` with one NULL and one INTEGER column/`ceil(INTEGER-or-REAL-column)`/`ceiling(INTEGER-or-REAL-column)`/`floor(INTEGER-or-REAL-column)`/`trunc(INTEGER-or-REAL-column)`/`sqrt`/`exp`/`ln`/`log`/`log10`/`log2`/`acos`/`asin`/`atan`/`cos`/`sin`/`tan`/`cosh`/`sinh`/`tanh`/`acosh`/`asinh`/`atanh`/`radians`/`degrees` over an INTEGER-or-REAL column, and either-argument-order signed-integer/float/NULL-constant `pow`/`power`/`mod`/`atan2`/binary-`log` with one INTEGER-or-REAL column/`typeof(column)`/`octet_length(TEXT-or-BLOB-column)`/`length(TEXT-or-BLOB-column)`/`unicode(TEXT-or-BLOB-column)`/`trim`/`ltrim`/`rtrim` with one TEXT column and an optional NULL argument/`substr`/`substring` with one TEXT-or-BLOB column and signed-integer/float/NULL remaining arguments/`concat` with one TEXT column and surrounding NULL arguments/either-order column-NULL `||` and NULL-propagating arithmetic/bitwise/relational operators/`likely(column)`/`unlikely(column)`/`likelihood(column,constant-0-to-1)`, and either-order signed-integer/float/NULL-constant `min`/`max` with one INTEGER-or-REAL column and either-order signed-integer/float/NULL-constant `ifnull`/`coalesce`/`nullif` with one column, `column IS [NOT] NULL`/`column ISNULL`/`column NOTNULL`, and either-order INTEGER-or-REAL-column/signed-integer-or-float comparison/`IS [NOT] signed-integer-or-float-value`/`IS [NOT] DISTINCT FROM signed-integer-or-float-value` aliases/mixed signed-integer-or-float `[NOT] BETWEEN`/two-value `[NOT] IN` and TEXT-affinity-or-INTEGER operator/function-form `[NOT] LIKE`/`[NOT] GLOB` expression indexes, plus bounded recursively parenthesized and unary-NOT-negated one- to eight-term `AND`/`OR` combinations with SQL three-valued `AND`/`OR`/`NOT` semantics for constant integer/float/NULL truth terms and either-order NULL comparisons, `WHERE column IS [NOT] NULL`/`column ISNULL`/`column NOTNULL` or `NULL IS [NOT] column`, either-order INTEGER-or-REAL-column/signed-integer-or-float `IS [NOT]`/`IS [NOT] DISTINCT FROM` aliases and signed-integer-or-float comparisons plus mixed signed-integer-or-float `[NOT] BETWEEN`/two-value `[NOT] IN`, bare/`NOT` INTEGER-or-REAL truth tests, and declared-or-explicit BINARY/NOCASE/RTRIM-TEXT-affinity either-order string-literal equality/inequality/relational-comparison/`[NOT] BETWEEN`/either-order `IS [NOT]`/`IS [NOT] DISTINCT FROM` aliases/two-value `[NOT] IN`/TEXT-affinity-or-INTEGER ASCII-NOCASE operator/function-form `[NOT] LIKE` with optional ASCII `ESCAPE`/case-sensitive operator/function-form `[NOT] GLOB` with ASCII character classes plus built-in BINARY/NOCASE/RTRIM and registered or UTF-8/UTF-16 factory-resolved application-collated mixed ASC/DESC indexes refill matching existing rows, enforce collation-aware UNIQUE with NULL/conflict rollback, maintain membership-changing direct and foreign-key-action INSERT/UPDATE/DELETE and atomically refill named indexes, all indexes of a named table, or registered-collation matches, all ordinary indexes, or the expression-index set through REINDEX under statement/transaction savepoints, supplies eligible scalar/composite foreign-key parent keys, reclaims indexes on DROP TABLE, executes main/attached INDEXED BY scans, and rejects writable incremental-blob opens on indexed or foreign-key child columns while preserving read-only indexed-column opens; do not claim attached triggers, broader expression indexes, or broader partial predicates until those paths have differential closure. The pinned native oracle rejects `IS TRUE`/`IS FALSE` in this bounded CREATE INDEX path and rejects a unary sign before the `likelihood()` probability literal, so do not broaden either syntax from general SQL-expression intuition. Signed-literal lookahead must distinguish minus-integer from minus-float before entering a parser branch; a generic minus-integer probe otherwise rejects valid negative-float partial comparisons before the float term parser runs. Partial-predicate boolean evaluation must retain SQL three-valued logic internally: ordinary comparisons with NULL and a constant NULL are unknown, NOT unknown remains unknown, and only a final true result includes the row. Schema-qualified virtual-table creation must key instances by schema, pass the selected schema as constructor argv[1], permit equal table names in main and attached databases, route qualified scans and drops to the matching instance, and xDisconnect/remove only that schema's instances before DETACH so reattachment may recreate them; do not claim sqlite_schema persistence or reopen integration until those paths exist. The TEMP schema lazily owns a private connection-local database, CREATE TEMP TABLE stores normalized ordinary schema SQL there, and unqualified table lookup searches TEMP before main and attached schemas for SELECT/INSERT/UPDATE/DELETE/DROP; explicit `temp.` lookup must accept the TEMP keyword as a schema identifier. Ordinary indexes on TEMP tables live in that database, and source `sqlite3FindIndex()` order makes unqualified DROP INDEX choose TEMP before same-named main and attached indexes. Bounded BEGIN/COMMIT/ROLLBACK retains per-database mutation batches across statements, reports autocommit accurately, validates deferred foreign keys at COMMIT, leaves a failed deferred COMMIT active for repair, and supports attached deferred-NO-ACTION continuation, wraps each explicit-transaction DML statement in a Pager savepoint so an immediate constraint rolls back only that statement and later COMMIT preserves prior work, and keeps CREATE/DROP TABLE inside the owning explicit transaction so rollback restores schema/content while commit preserves it; do not claim atomic commit across multiple databases until that path exists. Resolve connection-local eponymous table functions before schema requirements and accept keyword-fallback column tokens, not only `TK_ID`. When a source-faithful replacement invalidates a simplified test expectation, confirm the upstream body and update the stale test rather than restoring the substitute behavior. Pager calls through the typed memory-journal owner must pass a valid SQLite sync mode such as `SQLITE_SYNC_NORMAL`; zero reaches post-spill VFS assertions and is not a valid substitute. Never claim counts, guarantees, completion, integration, or evidence not checked by an existing mechanism/artifact. Independent fidelity review is deferred and is not a current objective or gate.

7. **Recovery.** After compaction, session recovery, or user correction, read this file and `upstream/active-port-batch.json`; repair stale instructions before acting.

8. **Pre-compilation gate.** Checkpoint promotion, not ordinary compilation, enforces the batch threshold. Ledger only already researched/implemented exact IDs before the first compile. Before every compile (including direct `zig test`), scan every currently uncompiled production change in the workspace, not only the task's edited files:
   - scan multiline and same-line `for`/`while`/optional- or error-capture/nested-`if` assignment chains (including `catch |e| assignment`; use `rg --pcre2` when a pattern contains look-around); brace nested branches and loop/conditional chains;
   - collect only column-zero top-level function/const/var names first (including `extern fn` declarations), reject duplicate top-level names before converting them to a set, then compare against every parameter, local binding, and capture, including test-local names; also compare method names against captures and all local bindings in the same container and compare every nested local binding against all lexical outer bindings in its function because Zig rejects both forms of shadowing. Collision scanners must parse balanced signatures without treating return types or nested struct methods as parameter names, must parse capture syntax instead of mistaking bitwise/logical `|` operators for captures, must distinguish local `const`/`var` declarations from pointer qualifiers such as `*const Type`, and indentation matches use horizontal `[ \t]` rather than `\s` so they cannot consume newlines; after changing or removing any function or method, search all production and test call sites before compiling, including unqualified references inside its defining file;
   - verify imports and reject reserved identifiers; unwrap nullable C-layout callback pointers before invocation even when source invariants guarantee non-null;
   - keep non-mutated locals `const` (retain `var` when owned fields are mutated/deinitialized); remove pointless discards; never discard an error-set value;
   - map allocator `OutOfMemory` into bounded local errors; give mutable `std.math.maxInt(...)` values an explicit integer type; compare arrays with `std.mem.eql`/element checks and inspect optional error sets by capture instead of direct `==`; audit lazy connection-owned allocation failure plus every constructor/destructor path; container-level comptime initializers must not mutate through pointer operands, so defer pointed-to runtime state updates to an invoked path;
   - use supported pinned-Zig operations: no `@fence`, C-only escapes, nonexistent trim/time helpers (the pinned standard library has no `std.time.milliTimestamp` or `std.fs.cwd()`; use `std.Io.Dir.cwd()` with an explicit `Io` such as `std.testing.io`), parent-relative/absolute temporary imports, `std.Thread.Mutex` (use the repository mutex), or `std.testing.refAllDeclsRecursive`; assert alignment with `@alignCast` before converting `dlsym` data pointers to function pointers;
   - require integer-switch `else => unreachable`; after extending any enum or tagged union, mechanically audit every exhaustive switch over it before compiling; when inserting adjacent switch prongs by replacing the next prong's anchor, reread the boundary and preserve the next prong's complete `return switch (...) {` opener. Use an explicitly typed shift amount for runtime integer shifts, `u21` before Unicode encoding, explicit unsigned-to-signed API casts, `.ptr` when passing slices to raw many-pointer APIs (including varint decoders), `&functionName` before pointer-cast, comptime format strings with mechanically matched placeholder/argument counts, spaces around `catch`/`and`/`or`, and `slice, start..` offset loops;
   - ensure conditional-expression branches share a type and both referenced enum members exist even when one branch is unreachable at runtime; name cross-boundary struct/enum/result types; never use `Struct.field` as a type; avoid anonymous result structs inferred only from `null`; give mutually recursive functions an explicit shared error set to break inferred-error-set dependency loops; use `@enumFromInt` after explicit range validation instead of unavailable standard-library enum conversion helpers; match `catch` switches only against errors actually present in the inferred or declared error set, and remove stale `Misuse` arms after making a required database pointer explicit; public frontend tests use the `statement.sqlite3_stmt` type and `statement.sqlite3_step`/column/finalize functions, not undeclared local aliases; native differential clients configure variadic `sqlite3_db_config()` operations through the typed `zig_sqlite3_db_config_flag` seam because Zig cannot export the C varargs symbol; remember that a Zig sentinel-array length excludes its sentinel (`"x".*` is `[1:0]u8`), and initialize `Mem` with type-mask flags before adding dynamic/static/ephemeral ownership flags;
   - when replacing a whole function, inventory and recheck intervening helpers/types; for stateful cursor tests assert page/index and actual cell length immediately before overwrite.

9. **Ledger/hash repair.** Never refresh fidelity hashes merely to silence staleness. Regenerate the Zig declaration inventory only after the final production and test edits, before comparing or repairing ledger hashes; any later edit invalidates that repair sequence. If source-ledger verification reports one changed target, mechanically enumerate every changed mapped declaration (including brace-only edits), inspect diffs/classifications together, then make one consolidated update and one targeted rerun. When moving an exported declaration between modules, update the mapped declaration ID as well as its hash.

10. **Repository ownership and documentation.** The validated recovery baseline has a durable post-initial Git checkpoint; preserve that recoverable baseline and require the normal evidence gates for later checkpoints. Every script, generator, verifier, build gate, maintained document, and test-result artifact/module needs a current purpose, real invocation/reference owner, and non-duplicated authority. Before adding any helper, exact-search its intended declaration name and reuse or relocate an existing helper rather than relying on the later duplicate-name gate. Remove/consolidate orphaned, superseded, redundant, unverifiable, or bloated material. `docs/README.md` is the canonical documentation registry and complete maintained-document index; `tools/verify_docs.py` derives inventory only from purpose-table rows (not incidental prose links) and enforces unique purposes, references, documented build targets/result owners, and synchronized facts. This file references that registry instead of duplicating all document names. Mutable facts have one named machine owner and one prose summary at most.

11. **Safe editing/tooling.** Treat all `/proc` disappearance races as normal process exit. Preflight every bulk migration target and repeated helper call. Every multi-entry or parallel multi-file edit must be immediately preceded by an exact fixed-string count or read of each complete `oldText` in its target. Do not extrapolate adjacent code, rely on remembered spelling, or assume synchronized prose; a prior broad read is not a preflight. The edit tool never performs global replacement: when a fixed-string count exceeds one, enumerate uniquely contextualized replacements for every intended occurrence. Include call-site context when text is non-unique. Before editing one-line loops, count the exact old text using quoted Python when shell metacharacters occur. After deliberately introducing the same line in multiple functions, never target that line alone; include each function signature or adjacent unique context in one multi-entry edit. Avoid variable-width regex lookbehind; use captures or post-match filtering. Merge nearby replacements; read the exact tail before appending. Pass only Zig to `zig fmt`; use JSON-aware validation for JSON. Multi-file JSON migrations must compute and validate every in-memory result before writing any file; a late assertion in a direct-write script can otherwise leave a partially migrated ledger. Before writing any JSON consumer, including one-off repository queries, inspect the actual container type and exact keys at every accessed nesting level in a separate completed query instead of guessing collection or status-summary keys; `upstream/behavioral-inventory.json` stores records in `blocks`, not `functions`. When sampling heterogeneous lists, check the element type before treating it as an object. Sort records with an explicit scalar key rather than tuples that can fall through to comparing dictionaries on ties. Internal modules importing parent siblings must compile through a `src/core/` root wrapper in the repository build graph, then be passed as a named module; never make an `src/core/internal/` file the module root because Zig rejects its parent-relative imports. Temporary probes import repository sources as named modules (`--dep name -Mname=src/...`), never parent-relative or `/tmp` absolute imports. Differential runners must surface captured worker stdout/stderr when a worker exits by signal so assertion failures remain diagnosable. Do not invoke direct `zig test` on production roots that transitively import `build_profile`; compile them through the repository build graph, which supplies that named module. Parallel Clang oracle builds require distinct project-local `TMPDIR` directories because Clang's deterministic `/tmp/sqlite3-*.o` names collide across build targets.
