# C oracle

`sqlite3.c` is generated from the pinned canonical source with the definitions in `upstream/SQLITE_BUILD_PROFILE.json`. It is distribution/oracle material, not the source used for semantic porting.

Checksums are recorded in the build profile. Regenerate in a clean out-of-tree canonical build and fail if the output changes unexpectedly.
